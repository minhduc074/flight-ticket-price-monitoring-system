{
  "name": "fly-ticket-noti-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-server": "npm install --workspace=server",
    "build": "npm install --workspace=server && npm run prisma:generate --workspace=server",
    "vercel-build": "npm install --workspace=server && npx prisma generate --schema=server/prisma/schema.prisma"
  },
  "workspaces": [
    "server"
  ],
  "dependencies": {}
}
